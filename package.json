{
  "name": "air-quality-app",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc --skipLibCheck && mkdir -p dist/public && cp -r src/public/* dist/public/ || true",
    "start": "bun dist/index.js",
    "prestart": "bun run build"
  },
  "dependencies": {
    "@augmentos/sdk": "^1.1.4",
    "bun-types": "^1.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/express": "^4.17.17"
  }
}